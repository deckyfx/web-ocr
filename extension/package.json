{
  "name": "selfhost-ocr-extension",
  "version": "1.4.16",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run typecheck && bun run build.ts",
    "build:dev": "bun run build.ts",
    "typecheck": "bun run --cwd ../server types:api && tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/chrome": "^0.0.323",
    "elysia": "1.4.30",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@elysiajs/eden": "1.4.9",
    "tesseract.js": "^7.0.0"
  }
}
