@page "/"
@rendermode @(new InteractiveServerRenderMode(prerender: false))
@inject IJSRuntime JS
@implements IAsyncDisposable

@* React mounts into this div; Blazor manages its lifetime via JS Interop *@
<div @ref="_root"></div>

@code {
    private ElementReference _root;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
            await JS.InvokeVoidAsync("ReactBridge.mount", _root);
    }

    public async ValueTask DisposeAsync()
    {
        try
        {
            await JS.InvokeVoidAsync("ReactBridge.unmount", _root);
        }
        catch (JSDisconnectedException)
        {
            // Circuit closed (page navigated away) — React already GC'd
        }
    }
}
